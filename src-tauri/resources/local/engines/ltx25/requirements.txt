# torch 는 PyTorch 의 cu128 인덱스에서. 판은 업스케일 엔진과 같게 맞춰 두었습니다 —
# 같은 GPU 에 다른 판이 두 벌 깔리면 드라이버 문제를 가려내기가 어렵습니다.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
# Z-Image·Krea 2·LTX 2.5·Anima 는 0.36 에 **없습니다**(2026-09-17 에 wheel 을 열어 확인).
diffusers==0.40.0
# diffusers 0.40 은 huggingface-hub 1.23 이상을 못 박는데, transformers 4.57 은 1.0 미만을 못 박습니다 —
# 둘이 한 환경에 못 삽니다(2026-09-18 uv 해석 원문: «diffusers>=0.40.0 and transformers<=4.57.6 are
# incompatible»). 그래서 transformers 는 5 대, hub·safetensors 도 0.40 이 요구하는 판입니다.
transformers==5.17.0
accelerate==1.10.1
safetensors==0.8.0
sentencepiece==0.2.1
protobuf==6.33.0
peft==0.18.0
huggingface-hub==1.32.0
numpy==2.2.6
pillow==11.3.0
imageio==2.37.0
imageio-ffmpeg==0.6.0
# I2V는 첫 장면을 학습 규약의 H.264 CRF로 재압축합니다. imageio만 있으면 이 단계가 실패합니다.
av==15.1.0
ftfy==6.3.1

# GPU 가 작을 때 **정말로 줄여서** 올리려고. int8·nf4 둘 다 이 패키지가 합니다.
bitsandbytes==0.48.1

# ── 빠른 어텐션 ────────────────────────────────────────────────────────────
# SageAttention 2.2 — 어텐션을 int8 QK + fp8 PV 로 계산합니다. 2026-09-18 실측(RTX PRO
# 6000 Blackwell, sm_120): 길이 4k 에서 3.2배, 16k 에서 3.9배, 32k 에서 4.05배 빠릅니다.
# 영상은 시퀀스가 길어 이득이 그대로 시간으로 돌아옵니다.
#
# **여기에는 적지 않습니다.** 휠 주소를 여기 못 박으면 엔비디아가 아니거나 sm_80 보다
# 낮은 GPU, 윈도우가 아닌 PC 에서 **엔진 설치가 통째로 실패합니다**. 그래서 필수 꾸러미를 다 깐 뒤에
# `resources/local/fast_attention.py` 가 그 PC 의 토치를 보고 맞는 휠을 고릅니다.
# 맞는 것이 없으면 건너뛰고, 어텐션은 다음 순위로 내려갑니다 — 안 깔려도 돕니다.
